{
  "build": {
    "env": {
      "DATABASE_URL": "mysql://root:password@localhost:3306/placeholder"
    }
  },
  "env": {
    "DATABASE_URL": "mysql://root:password@localhost:3306/placeholder"
  },
  "crons": [
    {
      "path": "/api/admin/sync",
      "schedule": "0 2 * * *"
    }
  ]
}
